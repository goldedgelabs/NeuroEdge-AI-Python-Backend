// Example rate limit using Upstash REST API (simple)
export async function rateLimit(key, limit=60, window=60){
  // This is a conceptual snippet; implement with your Upstash REST token
  // Use fetch to call Upstash incr with expiry
  return true;
}
